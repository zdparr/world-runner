services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: narrator
      POSTGRES_PASSWORD: narrator
      POSTGRES_DB: narrator
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U narrator"]
      interval: 5s
      timeout: 3s
      retries: 10
volumes:
  pgdata:
